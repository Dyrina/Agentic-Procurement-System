services:
  frontend:
    build:
      context: ./frontend
    image: procurement-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    image: procurement-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env